using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public interface CanvasData
{
    // Start is called before the first frame update

    public TMP_Text PingPanel { set; }
  //void SetPing(string ping);

 
}
